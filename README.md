# Автоматизация тестирования effective-mobile.ru

Проект автоматизации UI-тестирования главной страницы сайта [effective-mobile.ru](https://effective-mobile.ru) с использованием Python, Playwright, паттерна Page Object и Allure отчетности.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Установка и настройка](#установка-и-настройка)
- [Запуск тестов](#запуск-тестов)
- [Docker](#docker)
- [Allure отчеты](#allure-отчеты)

## 🎯 Описание проекта

Проект содержит автоматизированные UI-тесты для проверки навигации на главной странице сайта effective-mobile.ru. Тесты проверяют корректность переходов по основным блокам сайта:
- О нас
- Услуги
- Проекты
- Отзывы
- Контакты

## 🛠 Технологический стек

- **Python 3.11+** - язык программирования
- **Playwright** - фреймворк для автоматизации браузера
- **Pytest** - фреймворк для тестирования
- **Allure** - инструмент для генерации отчетов
- **Docker** - контейнеризация тестов
- **Page Object Model** - паттерн проектирования

## 📁 Структура проекта

```
fictional-giggle/
│
├── tests/
│   ├── __init__.py
│   ├── test_navigation.py          # UI тесты навигации
│   └── conftest.py                  # Pytest конфигурация и фикстуры
│
├── pages/
│   ├── __init__.py
│   ├── base_page.py                 # Базовый класс Page Object
│   └── main_page.py                 # Page Object главной страницы
│
├── locators/
│   ├── __init__.py
│   └── main_page_locators.py        # Локаторы элементов
│
├── utils/
│   ├── __init__.py
│   └── helpers.py                   # Вспомогательные функции
│
├── allure-results/                  # Результаты тестов
├── allure-report/                   # HTML отчеты Allure
│
├── Dockerfile                       # Docker конфигурация
├── requirements.txt                 # Python зависимости
├── pytest.ini                       # Настройки pytest
├── .gitignore
└── README.md
```

## ⚙️ Требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)
- Docker (опционально, для запуска в контейнере)
- Allure Command Line (для генерации отчетов)

## 🚀 Установка и настройка

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/inspectorrrrr/fictional-giggle
cd fictional-giggle
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**

*Windows:*
```bash
venv\Scripts\activate
```

*Linux/macOS:*
```bash
source venv/bin/activate
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

5. **Установите браузеры Playwright:**
```bash
playwright install chromium
```

Для установки всех браузеров:
```bash
playwright install
```

### Установка Allure (для генерации отчетов)

**macOS:**
```bash
brew install allure
```

**Windows (с Scoop):**
```bash
scoop install allure
```

**Linux:**
```bash
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update 
sudo apt-get install allure
```

Или скачайте с [официального сайта](https://docs.qameta.io/allure/).

## 🧪 Запуск тестов

### Запуск всех тестов

```bash
pytest tests/ -v
```

### Запуск с Allure отчетом

```bash
pytest tests/ -v --alluredir=allure-results
```

### Запуск конкретного теста

```bash
pytest tests/test_navigation.py::TestMainPageNavigation::test_navigate_to_about_section -v
```

### Запуск с выбором браузера

```bash
pytest tests/ -v --browser=chromium
pytest tests/ -v --browser=firefox
pytest tests/ -v --browser=webkit
```

### Запуск в headed режиме (видимый браузер)

```bash
pytest tests/ -v --headed
```

### Параллельный запуск тестов

```bash
pytest tests/ -v -n auto --alluredir=allure-results
```

## 🐳 Docker

### Сборка Docker образа

```bash
docker build -t fictional-giggle .
```

### Запуск тестов в Docker

```bash
docker run --rm -v $(pwd)/allure-results:/app/allure-results fictional-giggle
```

### Запуск с пробросом результатов на хост

*Linux/macOS:*
```bash
docker run --rm \
  -v $(pwd)/allure-results:/app/allure-results \
  fictional-giggle
```

*Windows (PowerShell):*
```bash
docker run --rm `
  -v ${PWD}/allure-results:/app/allure-results `
  fictional-giggle
```

### Запуск с пользовательской командой

```bash
docker run --rm fictional-giggle pytest tests/test_navigation.py::TestMainPageNavigation::test_navigate_to_about_section -v
```


## 📊 Allure отчеты

### Генерация и просмотр отчета

После запуска тестов с опцией `--alluredir=allure-results`:

```bash
# Генерация отчета
allure generate allure-results -o allure-report --clean

# Открытие отчета в браузере
allure open allure-report
```

### Быстрый просмотр отчета

```bash
allure serve allure-results
```

Эта команда автоматически сгенерирует отчет и откроет его в браузере.
